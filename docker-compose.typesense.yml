version: '3.8'

services:
  typesense:
    image: typesense/typesense:27.1
    container_name: workcrew-typesense
    ports:
      - "8108:8108"
    volumes:
      - typesense-data:/data
    command: '--data-dir /data --api-key=xyz --enable-cors'
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8108/health"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - workcrew-network

volumes:
  typesense-data:

networks:
  workcrew-network:
    driver: bridge