---
phase: 03-proctoring-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["prisma/schema.prisma", "src/components/Assessment.tsx"]
autonomous: true
requirements: ["PROCTOR-01", "PROCTOR-02"]
user_setup: []
must_haves:
  truths:
    - "Candidates cannot easily switch tabs or right-click during assessments"
    - "Basic suspicious behaviors are logged"
    - "Assessment integrity monitoring is active"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Proctoring event logging models"
      contains: "model ProctoringEvent"
    - path: "src/components/Assessment.tsx"
      provides: "Browser lockdown and monitoring"
      min_lines: 50
  key_links:
    - from: "src/components/Assessment.tsx"
      to: "browser APIs"
      via: "fullscreen and event listeners"
      pattern: "requestFullscreen|addEventListener"
    - from: "src/components/Assessment.tsx"
      to: "prisma.proctoringEvent"
      via: "API call on suspicious activity"
      pattern: "fetch.*proctoring"
---

<objective>
Implement database schema and basic proctoring monitoring for cheating-resistant assessments

Purpose: Establish foundation for monitoring layer to detect and prevent cheating
Output: Proctoring data models and browser lockdown functionality
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-proctoring-system/DISCOVERY.md
@.planning/phases/02-assessment-engine/02-02-SUMMARY.md
@src/components/Assessment.tsx
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add proctoring database models</name>
  <files>prisma/schema.prisma</files>
  <action>Add ProctoringEvent and ProctoringFlag models to schema.prisma. ProctoringEvent should track events like tab_switch, right_click, copy_attempt with timestamp, assessmentId, candidateId. ProctoringFlag should store flagged assessments with reason and severity. Run prisma db push after changes.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Schema contains ProctoringEvent and ProctoringFlag models, migration applied</done>
</task>

<task type="auto">
  <name>Task 2: Implement browser lockdown in Assessment component</name>
  <files>src/components/Assessment.tsx</files>
  <action>Modify Assessment.tsx to request fullscreen on assessment start, prevent right-click context menu, detect tab visibility changes, and log suspicious events to API. Add event listeners for visibilitychange, contextmenu, and fullscreen exit attempts. Use browser APIs like requestFullscreen, addEventListener.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Assessment component enters fullscreen, blocks right-click, detects tab switches</done>
</task>

<task type="auto">
  <name>Task 3: Add basic behavior monitoring</name>
  <files>src/components/Assessment.tsx</files>
  <action>Extend Assessment component to monitor keystrokes for rapid paste detection (time between keydown events), track mouse movements for unusual patterns, and send events to /api/proctoring/events endpoint. Implement simple heuristics for suspicious activity flagging.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Keystroke and mouse monitoring active, events logged to database</done>
</task>

</tasks>

<verification>
Run assessment in browser, verify fullscreen activates, right-click blocked, tab switch detected and logged
</verification>

<success_criteria>
Proctoring foundation in place: schema supports event logging, UI enforces basic lockdown, suspicious behaviors detected
</success_criteria>

<output>
After completion, create .planning/phases/03-proctoring-system/03-01-SUMMARY.md
</output>