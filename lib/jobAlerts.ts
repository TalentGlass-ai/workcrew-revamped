// JobAlert model not yet in schema — this function is a no-op until the model is added
export async function processJobAlerts(): Promise<void> {
  console.log('[jobAlerts] JobAlert model not yet in schema, skipping')
}
