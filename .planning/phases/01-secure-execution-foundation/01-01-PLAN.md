---
phase: 01-secure-execution-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["prisma/schema.prisma"]
autonomous: true
requirements: ["DB-01", "DB-02", "DB-03", "DB-04", "DB-05"]
user_setup: []

must_haves:
  truths:
    - Database schema includes Assessment model with score, difficulty, report fields
    - ProctoringLog model exists for behavior tracking
    - Assessment relationships added to Candidate and Job models
    - Assessment metadata fields (language, time taken, attempts) included
    - Proper indexing implemented for assessment queries
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Assessment and ProctoringLog models"
      contains: "model Assessment", "model ProctoringLog"
  key_links:
    - from: "Assessment"
      to: "Candidate"
      via: "candidateId"
      pattern: "candidateId"
    - from: "Assessment"
      to: "Job"
      via: "jobId"
      pattern: "jobId"

---

<objective>
Implement database schema for assessment system

Purpose: Provide data foundation for assessments and proctoring
Output: Updated Prisma schema with new models and relationships
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Assessment model to schema</name>
  <files>prisma/schema.prisma</files>
  <action>Add Assessment model with fields: id (String @id @default(cuid())), candidateId (String), jobId (String?), score (Float?), difficulty (String), report (Json), language (String), timeTaken (Int), attempts (Int @default(1)), createdAt (DateTime @default(now())), updatedAt (DateTime @updatedAt). Use appropriate types and relations.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Assessment model added to schema</done>
</task>

<task type="auto">
  <name>Task 2: Add ProctoringLog model</name>
  <files>prisma/schema.prisma</files>
  <action>Add ProctoringLog model with fields: id (String @id @default(cuid())), assessmentId (String), eventType (String), timestamp (DateTime @default(now())), details (Json?). Add relation to Assessment.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>ProctoringLog model added to schema</done>
</task>

<task type="auto">
  <name>Task 3: Update relationships and run migration</name>
  <files>prisma/schema.prisma</files>
  <action>Add relations: Assessment belongs to Candidate (candidateId), optional to Job (jobId). Add indexes on candidateId, jobId, createdAt. Ensure Candidate and Job models exist or add placeholders.</action>
  <verify>
    <automated>npx prisma migrate dev --name add-assessment-models</automated>
  </verify>
  <done>Migration applied, schema updated</done>
</task>

</tasks>

<verification>
Run `npx prisma studio` to verify models and relationships
</verification>

<success_criteria>
Database schema supports assessment data with proper relationships and indexing
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-execution-foundation/01-01-SUMMARY.md`
</output></content>
<parameter name="filePath">/Volumes/Macintosh HD - Data/WorkCrew - Job Portal/workcrew-revamped/.planning/phases/01-secure-execution-foundation/01-01-PLAN.md