---
phase: 04-skill-intelligence-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/skillProcessor.ts", "src/app/api/assessments/[id]/results/route.ts", "src/lib/skillGraphService.ts", "prisma/schema.prisma"]
autonomous: true
requirements: ["SKILL-01", "SKILL-02", "SKILL-03", "SKILL-04", "SKILL-05"]
user_setup: []

must_haves:
  truths:
    - "Assessment results update candidate's validated skills"
    - "Skills from assessments are marked as validated"
    - "Skill strengths reflect assessment performance"
    - "Assessment data integrates with skill graph for better matching"
  artifacts:
    - path: "src/lib/skillProcessor.ts"
      provides: "Assessment result processing and skill extraction"
    - path: "src/app/api/assessments/[id]/results/route.ts"
      provides: "Assessment completion API with skill integration"
    - path: "src/lib/skillGraphService.ts"
      provides: "Skill graph database integration"
    - path: "prisma/schema.prisma"
      provides: "Updated CandidateSkill model with validation fields"
  key_links:
    - from: "Assessment completion"
      to: "skillProcessor.updateSkills()"
      via: "API results endpoint"
      pattern: "assessment.*results.*skillProcessor"
    - from: "skillProcessor"
      to: "CandidateSkill"
      via: "Prisma update"
      pattern: "prisma.candidateSkill.*update"
    - from: "skillProcessor"
      to: "skillGraphService"
      via: "Graph database push"
      pattern: "neo4j.*pushAssessmentData"

---

<objective>
Connect assessment results to skill graph database to enable validated skills for enhanced candidate matching.

Purpose: Transform assessment performance into validated skill data that integrates with the skill intelligence system for better job-candidate matching.

Output: Assessment completion automatically updates candidate skills with validation status and pushes data to graph database.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-assessment-engine/02-02-SUMMARY.md
@.planning/phases/03-proctoring-system/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CandidateSkill model for validation</name>
  <files>prisma/schema.prisma</files>
  <action>Add validation fields to CandidateSkill model: isValidated (Boolean), validatedAt (DateTime), validationSource (String). Update the model to support marking skills as validated vs self-reported.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>CandidateSkill model updated with validation fields</done>
</task>

<task type="auto">
  <name>Task 2: Create skill processor service</name>
  <files>src/lib/skillProcessor.ts</files>
  <action>Implement SkillProcessor class with updateSkillsFromAssessment method that: extracts skills from assessment questions/answers, calculates skill scores based on performance, updates CandidateSkill records with validation data, and calls skill graph service for graph updates.</action>
  <verify>
    <automated>npm test -- src/lib/skillProcessor.test.ts</automated>
  </verify>
  <done>Skill processor service created with unit tests</done>
</task>

<task type="auto">
  <name>Task 3: Integrate with assessment results API</name>
  <files>src/app/api/assessments/[id]/results/route.ts</files>
  <action>Modify the assessment results API to call skillProcessor.updateSkillsFromAssessment after assessment completion, ensuring skills are updated automatically when results are retrieved.</action>
  <verify>
    <automated>curl -X GET http://localhost:3000/api/assessments/test-id/results | grep "skills_updated"</automated>
  </verify>
  <done>Assessment results API triggers skill updates</done>
</task>

<task type="auto">
  <name>Task 4: Implement skill graph service</name>
  <files>src/lib/skillGraphService.ts</files>
  <action>Create SkillGraphService to connect to Neo4j graph database and push assessment data: add validated skill nodes, update relationships based on co-occurrence in assessments, enhance matching algorithms with validated data.</action>
  <verify>
    <automated>npm test -- src/lib/skillGraphService.test.ts</automated>
  </verify>
  <done>Skill graph service pushes assessment data to Neo4j</done>
</task>

</tasks>

<verification>
Run full assessment flow: create assessment, submit answers, verify skills updated in database and graph.
</verification>

<success_criteria>
Assessment completion results in updated validated skills, marked as validated, with strengths reflecting performance, and data pushed to skill graph database.
</success_criteria>

<output>
After completion, create .planning/phases/04-skill-intelligence-integration/04-01-SUMMARY.md
</output></content>
<parameter name="filePath">/Volumes/Macintosh HD - Data/WorkCrew - Job Portal/workcrew-revamped/.planning/phases/04-skill-intelligence-integration/04-01-PLAN.md