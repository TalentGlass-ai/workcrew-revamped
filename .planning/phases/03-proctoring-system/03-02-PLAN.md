---
phase: 03-proctoring-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: ["src/components/Assessment.tsx", "src/app/api/proctoring/route.ts", "src/components/RecruiterDashboard.tsx"]
autonomous: true
requirements: ["PROCTOR-03", "PROCTOR-04"]
user_setup: []
must_haves:
  truths:
    - "Candidates verify identity via webcam at assessment start"
    - "Advanced suspicious behaviors trigger flags"
    - "Recruiters can review flagged assessments in dashboard"
  artifacts:
    - path: "src/components/Assessment.tsx"
      provides: "Webcam verification and advanced monitoring"
      min_lines: 100
    - path: "src/app/api/proctoring/route.ts"
      provides: "Proctoring event API and flagging logic"
      exports: ["POST /api/proctoring/events", "GET /api/proctoring/flags"]
    - path: "src/components/RecruiterDashboard.tsx"
      provides: "Flagged assessments review interface"
      min_lines: 30
  key_links:
    - from: "src/components/Assessment.tsx"
      to: "getUserMedia API"
      via: "webcam access on start"
      pattern: "getUserMedia|video"
    - from: "src/app/api/proctoring/route.ts"
      to: "prisma.proctoringFlag"
      via: "create flag on suspicious patterns"
      pattern: "prisma\\.proctoringFlag\\.create"
    - from: "src/components/RecruiterDashboard.tsx"
      to: "/api/proctoring/flags"
      via: "fetch flagged assessments"
      pattern: "fetch.*proctoring/flags"
---

<objective>
Implement advanced proctoring features and recruiter review dashboard

Purpose: Add identity verification and automated flagging with recruiter oversight
Output: Complete monitoring layer with webcam check and flagged assessment management
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-proctoring-system/03-01-PLAN.md
@.planning/phases/02-assessment-engine/02-02-SUMMARY.md
@src/components/Assessment.tsx
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add webcam identity verification</name>
  <files>src/components/Assessment.tsx</files>
  <action>Extend Assessment component to request camera permission on assessment start, display video feed for identity confirmation, and capture a snapshot for logging. Use getUserMedia API to access webcam, show video element, and allow candidate to confirm identity before proceeding.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Webcam access requested, video displayed for identity verification</done>
</task>

<task type="auto">
  <name>Task 2: Implement advanced behavior detection and flagging</name>
  <files>src/app/api/proctoring/route.ts</files>
  <action>Create /api/proctoring/events POST endpoint to receive monitoring events from client, analyze patterns (multiple rapid pastes, extended tab switches), and create ProctoringFlag records for suspicious activity. Implement GET /api/proctoring/flags for recruiter access.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>API endpoints created, flagging logic analyzes event patterns</done>
</task>

<task type="auto">
  <name>Task 3: Create recruiter flagged assessments dashboard</name>
  <files>src/components/RecruiterDashboard.tsx</files>
  <action>Create new RecruiterDashboard component that fetches flagged assessments from /api/proctoring/flags, displays them in a table with assessment details, flag reasons, and review actions (dismiss/clear flag). Integrate into existing recruiter interface.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Dashboard component renders flagged assessments with review options</done>
</task>

</tasks>

<verification>
Test full flow: start assessment with webcam verification, trigger suspicious behavior, verify flag appears in recruiter dashboard
</verification>

<success_criteria>
Complete proctoring system: identity verified, behaviors monitored and flagged, recruiters can review suspicious assessments
</success_criteria>

<output>
After completion, create .planning/phases/03-proctoring-system/03-02-SUMMARY.md
</output>