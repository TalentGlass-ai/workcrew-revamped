---
phase: 06-user-interfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/app/assessment/page.tsx", "src/app/assessment/[id]/page.tsx", "src/app/assessment/[id]/results/page.tsx"]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Candidate can browse available assessments"
    - "Candidate can start an assessment"
    - "Candidate can complete assessment with proctoring"
    - "Candidate can view assessment results"
  artifacts:
    - path: "src/app/assessment/page.tsx"
      provides: "Assessment list and start interface"
      min_lines: 30
    - path: "src/app/assessment/[id]/page.tsx"
      provides: "Assessment taking page"
      min_lines: 20
    - path: "src/app/assessment/[id]/results/page.tsx"
      provides: "Assessment results display"
      min_lines: 30
  key_links:
    - from: "src/app/assessment/page.tsx"
      to: "/api/assessments"
      via: "fetch available assessments"
      pattern: "fetch.*api/assessments"
    - from: "src/app/assessment/[id]/page.tsx"
      to: "src/components/Assessment.tsx"
      via: "import and render"
      pattern: "import.*Assessment"
    - from: "src/app/assessment/[id]/results/page.tsx"
      to: "/api/assessment/results"
      via: "fetch results"
      pattern: "fetch.*api/assessment/results"
---

<objective>
Build candidate assessment experience with start, take, and results pages

Purpose: Enable candidates to discover, take, and review assessments
Output: Working assessment workflow for candidates
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-layer/05-01-SUMMARY.md
@.planning/phases/02-assessment-engine/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assessment list page</name>
  <files>src/app/assessment/page.tsx</files>
  <action>Create Next.js page that fetches available assessments from GET /api/assessments, displays them in a list with titles, descriptions, and start buttons. Include loading and error states. Use Tailwind for styling.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Page renders assessment list with start buttons</done>
</task>

<task type="auto">
  <name>Task 2: Create assessment taking page</name>
  <files>src/app/assessment/[id]/page.tsx</files>
  <action>Create dynamic route page that extracts assessmentId from params, renders the Assessment component with the id. Handle loading states and redirect to results on completion.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Page loads and displays assessment for taking</done>
</task>

<task type="auto">
  <name>Task 3: Create assessment results page</name>
  <files>src/app/assessment/[id]/results/page.tsx</files>
  <action>Create page that fetches assessment results from GET /api/assessment/results?id={id}, displays score, detailed report, and questions with user answers and correct answers. Include navigation back to assessment list.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Page shows complete assessment results</done>
</task>

</tasks>

<verification>
Run npm run build to ensure all pages compile without errors
</verification>

<success_criteria>
Candidates can navigate from assessment list to taking an assessment to viewing results
</success_criteria>

<output>
After completion, create .planning/phases/06-user-interfaces/06-01-SUMMARY.md
</output></content>
<parameter name="filePath">/Volumes/Macintosh HD - Data/WorkCrew - Job Portal/workcrew-revamped/.planning/phases/06-user-interfaces/06-01-PLAN.md