---
phase: 05-api-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/app/api/assessment/start/route.ts", "src/app/api/assessment/submit/route.ts", "src/app/api/assessment/results/route.ts", "src/app/api/assessment/proctoring/route.ts", "src/app/api/assessment/candidate/[id]/route.ts"]
autonomous: true
requirements: ["API-01", "API-02", "API-03", "API-04", "API-05"]
user_setup: []

must_haves:
  truths:
    - "Candidate can start an assessment session"
    - "Candidate can submit code solutions"
    - "Candidate can retrieve assessment results"
    - "Proctoring events are logged during assessment"
    - "Recruiters can access candidate assessment history"
  artifacts:
    - path: "src/app/api/assessment/start/route.ts"
      provides: "Assessment session initialization"
      exports: ["POST"]
    - path: "src/app/api/assessment/submit/route.ts"
      provides: "Code solution submission and evaluation"
      exports: ["POST"]
    - path: "src/app/api/assessment/results/route.ts"
      provides: "Assessment results retrieval"
      exports: ["GET"]
    - path: "src/app/api/assessment/proctoring/route.ts"
      provides: "Proctoring event logging"
      exports: ["POST"]
    - path: "src/app/api/assessment/candidate/[id]/route.ts"
      provides: "Candidate assessment history"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/assessment/start/route.ts"
      to: "AssessmentAttempt"
      via: "create attempt"
      pattern: "prisma.assessmentAttempt.create"
    - from: "src/app/api/assessment/submit/route.ts"
      to: "Assessment"
      via: "update score and report"
      pattern: "prisma.assessment.update"
    - from: "src/app/api/assessment/results/route.ts"
      to: "Assessment"
      via: "findUnique with include"
      pattern: "prisma.assessment.findUnique"
    - from: "src/app/api/assessment/proctoring/route.ts"
      to: "ProctoringEvent"
      via: "create event"
      pattern: "prisma.proctoringEvent.create"
    - from: "src/app/api/assessment/candidate/[id]/route.ts"
      to: "Assessment"
      via: "findMany by candidateId"
      pattern: "prisma.assessment.findMany"
---

<objective>
Implement backend APIs for assessment workflow

Purpose: Enable the complete assessment lifecycle from start to results, with proctoring support
Output: RESTful API endpoints for assessment management
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-assessment-engine/02-02-SUMMARY.md
@.planning/phases/03-proctoring-system/03-01-SUMMARY.md
@prisma/schema.prisma
@src/lib/evaluator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement start and submit endpoints</name>
  <files>src/app/api/assessment/start/route.ts, src/app/api/assessment/submit/route.ts</files>
  <action>Implement POST /api/assessment/start to initialize assessment session by creating AssessmentAttempt with startedAt timestamp. Implement POST /api/assessment/submit to accept code solutions, evaluate using evaluator.ts, update Assessment score/report, and mark attempt as completed. Use existing Assessment and AssessmentAttempt models.</action>
  <verify>
    <automated>npm test -- --testPathPattern=assessment-api</automated>
  </verify>
  <done>Start endpoint returns assessment ID and questions, submit endpoint returns evaluation results</done>
</task>

<task type="auto">
  <name>Task 2: Implement results and candidate history endpoints</name>
  <files>src/app/api/assessment/results/route.ts, src/app/api/assessment/candidate/[id]/route.ts</files>
  <action>Implement GET /api/assessment/results to retrieve assessment results by ID, including score, report, and questions with answers. Implement GET /api/assessment/candidate/[id] to return paginated list of candidate's assessments with scores and completion status. Include proper error handling for unauthorized access.</action>
  <verify>
    <automated>npm test -- --testPathPattern=assessment-api</automated>
  </verify>
  <done>Results endpoint returns complete assessment data, candidate endpoint returns assessment history</done>
</task>

<task type="auto">
  <name>Task 3: Implement proctoring endpoint</name>
  <files>src/app/api/assessment/proctoring/route.ts</files>
  <action>Implement POST /api/assessment/proctoring to log proctoring events (tab_switch, copy_attempt, etc.) by creating ProctoringEvent records. Accept assessmentId, eventType, and details. Ensure events are timestamped and linked to assessment and candidate.</action>
  <verify>
    <automated>npm test -- --testPathPattern=assessment-api</automated>
  </verify>
  <done>Proctoring endpoint accepts and stores events without errors</done>
</task>

</tasks>

<verification>
Run npm test to verify all endpoints work correctly
</verification>

<success_criteria>
All API endpoints implemented and tested, assessment workflow complete from start to results
</success_criteria>

<output>
After completion, create .planning/phases/05-api-layer/05-01-SUMMARY.md
</output>